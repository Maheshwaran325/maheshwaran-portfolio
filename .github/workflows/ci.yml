name: CI

# Netlify deploys whatever lands on main. This runs first, so "it built" is not
# the only thing standing between a commit and the live site.

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - uses: actions/checkout@v7
        with:
          # The full history, for two reasons: /changelog is generated from
          # `git log`, and the sitemap's lastmod for each route comes from the
          # last commit that touched its sources. A shallow clone silently
          # turns both into "today".
          fetch-depth: 0

      - uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: npm

      - run: npm ci

      - name: Lint
        run: npm run lint

      - name: Typecheck and build
        run: npm run build

      - name: Install Chromium
        run: npx playwright install --with-deps chromium

      # The suite rebuilds and serves dist/ itself — see playwright.config.ts.
      - name: Test
        run: npm test

      - name: Lighthouse — mobile
        run: npx lhci autorun

      - name: Lighthouse — desktop
        run: npx lhci autorun --collect.settings.preset=desktop

      # Only when something went wrong: a green run has already said everything
      # it has to say, and 36 Lighthouse reports an hour would eat the account's
      # artifact storage for no reason.
      - name: Keep the reports
        if: failure()
        uses: actions/upload-artifact@v7
        with:
          name: reports
          path: |
            playwright-report/
            .lighthouseci/
          # .lighthouseci is a dotfile, and this action drops hidden paths
          # unless told otherwise — without this the Lighthouse reports are
          # silently left out of the artifact, which is the half you need.
          include-hidden-files: true
          retention-days: 14
          if-no-files-found: ignore
